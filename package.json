{
  "name": "sso-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:stack": "wrangler dev -c wrangler.toml -c ../auth-sync-consumer/wrangler.toml -c ../email-worker/wrangler.toml -c ../payments-worker/wrangler.toml -c ../realtime-worker/wrangler.toml",
    "deploy": "wrangler deploy",
    "generate-keys": "node scripts/generate-keys.mjs",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "2.4.3",
    "jose": "6.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260811.1",
    "@types/bcryptjs": "2.4.6",
    "supabase": "2.104.0",
    "typescript": "5.9.3",
    "vitest": "3.2.7",
    "wrangler": "4.120.1"
  }
}
