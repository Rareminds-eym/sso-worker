/**
 * Rate limiting (empty) — in-memory per-worker rate limiting removed per T-031.
 * KV-backed account lockout lives in src/lib/rate-limit.ts.
 * KV-backed endpoint rate limiting will be added in Phase 6 (T-049–T-053, T-057).
 */
